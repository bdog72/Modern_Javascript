// const myAge = 17;
// const message = myAge >= 18 ? 'You can vote' : 'You cannot vote';

// console.log(message);

// const myAge = 45;
// const showAge = () => 'Showing the page';

// const showErrorPage = () => {
//     console.log('Showing the error page');
// };

// const message = myAge >= 21 ? showAge() : showErrorPage();

// console.log(message);

const team = ['Tyler', 'Porter'];
console.log(
    team.length <= 4
        ? `Team size: ${team.length}`
        : 'Too many people on your team'
);
